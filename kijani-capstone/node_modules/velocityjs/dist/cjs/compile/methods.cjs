const require_prototype_guard = require("./prototype-guard.cjs");
//#region src/compile/methods.ts
function hasProperty(context, field) {
	if (typeof context === "number" || typeof context === "string") return context[field] || Object.prototype.hasOwnProperty.call(context, field);
	if (!context) return false;
	return field in context;
}
var matchProperty = (value, notInContext) => ({ property, context }) => value === property && (notInContext ? !hasProperty(context, property) : true);
var matchStartWith = (value) => ({ property, context }) => property.indexOf(value) === 0 && !(property in context) && property.length > value.length;
function getter(base, property) {
	if (typeof property === "number") return base[property];
	if (require_prototype_guard.isBlockedPrototypeKey(base, property)) return;
	const letter = property.charCodeAt(0);
	const isUpper = letter < 91;
	const ret = base[property];
	if (ret !== void 0) return ret;
	if (isUpper) property = String.fromCharCode(letter).toLowerCase() + property.slice(1);
	if (!isUpper) property = String.fromCharCode(letter).toUpperCase() + property.slice(1);
	if (require_prototype_guard.isBlockedPrototypeKey(base, property)) return;
	return base[property];
}
function setter(context, property, value) {
	const key = String(property);
	if (require_prototype_guard.isBlockedPrototypeKey(context, key, true)) return "";
	context[key] = value;
	return value;
}
function getSize(obj) {
	if (Array.isArray(obj)) return obj.length;
	if (typeof obj === "object" && obj !== null) return Object.keys(obj).length;
}
var handlers = {
	get: {
		match: matchProperty("get", true),
		resolve: ({ context, params }) => getter(context, params[0])
	},
	set: {
		match: matchProperty("set", true),
		resolve: ({ context, params }) => {
			setter(context, params[0], params[1]);
			return "";
		}
	},
	getValue: {
		match: matchStartWith("get"),
		resolve: ({ context, property }) => getter(context, property.slice(3))
	},
	isValue: {
		match: matchStartWith("is"),
		resolve: ({ context, property }) => getter(context, property.slice(2))
	},
	setValue: {
		match: matchStartWith("set"),
		resolve: ({ context, property, params }) => {
			setter(context, property.slice(3), params[0]);
			context.toString = () => "";
			return context;
		}
	},
	keySet: {
		match: matchProperty("keySet", true),
		resolve: ({ context }) => Object.keys(context)
	},
	entrySet: {
		match: matchProperty("entrySet", true),
		resolve: ({ context }) => Object.keys(context).map((key) => ({
			key,
			value: context[key]
		}))
	},
	size: {
		match: matchProperty("size", true),
		resolve: ({ context }) => getSize(context)
	},
	put: {
		match: matchProperty("put", true),
		resolve: ({ context, params }) => setter(context, params[0], params[1])
	},
	add: {
		match: matchProperty("add", true),
		resolve: ({ context, params }) => {
			if (typeof context.push !== "function") return;
			return context.push(params[0]);
		}
	},
	remove: {
		match: matchProperty("remove", true),
		resolve: ({ context, params }) => {
			if (Array.isArray(context)) {
				let index = params[0];
				if (typeof index !== "number") index = context.indexOf(params[0]);
				const ret = context[index];
				context.splice(index, 1);
				return ret;
			}
			if (typeof context === "object" && context !== null) {
				const ret = context[params[0]];
				delete context[params[0]];
				return ret;
			}
		}
	},
	subList: {
		match: matchProperty("subList", true),
		resolve: ({ context, params }) => context.slice(params[0], params[1])
	}
};
var methods_default = Object.keys(handlers).map((key) => ({
	uid: "system: " + key,
	match: handlers[key].match,
	resolve: handlers[key].resolve
}));
//#endregion
exports.default = methods_default;
