import { Compile } from "./compile/base-compile.mjs";
import { parse } from "./parse.mjs";
import { getRefText } from "./helper/index.mjs";
import "./compile/index.mjs";
//#region src/index.ts
var render = (template, context, macros, config) => {
	return new Compile(parse(template), config).render(context, macros);
};
var Helper = { getRefText };
var velocity = {
	render,
	parse,
	Compile,
	Helper
};
//#endregion
export { Compile, Helper, velocity as default, parse, render };
