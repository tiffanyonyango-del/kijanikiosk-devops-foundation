//#region src/compile/prototype-guard.ts
var PROTO_KEY = "__proto__";
var PROTOTYPE_CHAIN_KEYS = new Set(["constructor", "prototype"]);
function hasOwnProperty(baseRef, key) {
	return (typeof baseRef === "object" || typeof baseRef === "function") && baseRef !== null && Object.prototype.hasOwnProperty.call(baseRef, key);
}
function isBlockedPrototypeKey(baseRef, key, isEnd = false) {
	if (key === PROTO_KEY) return true;
	if (key === "prototype" && typeof baseRef === "function") return true;
	return !isEnd && PROTOTYPE_CHAIN_KEYS.has(key) && !hasOwnProperty(baseRef, key);
}
function hasBlockedUnknownPrototypePath(keys, startIndex) {
	return keys.slice(startIndex).some((key, i) => {
		const isEnd = startIndex + i === keys.length - 1;
		return key === PROTO_KEY || !isEnd && PROTOTYPE_CHAIN_KEYS.has(key);
	});
}
//#endregion
export { hasBlockedUnknownPrototypePath, hasOwnProperty, isBlockedPrototypeKey };
